name: Tassty CI with Codecov

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
    branches: [ "main", "develop" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Run Unit Tests & Coverage
        # Kita panggil task koverXmlReport secara spesifik
        run: ./gradlew :app:testDebugUnitTest :app:koverXmlReportDebug

      - name: Upload Report to Codecov
        uses: codecov/codecov-action@v4
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          # Kita kasih tau Codecov: "Cari semua file XML di folder build!"
          directory: ./app/build/reports/kover/
          fail_ci_if_error: true
          verbose: true